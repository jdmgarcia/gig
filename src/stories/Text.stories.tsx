import { storiesOf } from '@storybook/react';
import * as React from 'react';

storiesOf('Components/Text', module)
    .add('Simple example', () => {
        return (
            <span>
                Simple example
            </span>
        );
    });
